import { SipSlider } from "@/components/SipSlider";

const SipSliderWidget = () => {
  return (
    <div className="min-h-screen bg-background">
      <SipSlider />
    </div>
  );
};

export default SipSliderWidget;